services:
  web:
    image: node:22
    working_dir: /app
    volumes:
      - ./:/app
    command: sh -c "npm install && npx vite --host 0.0.0.0 --port 5173"
    ports:
      - "3000:5173"
    environment:
      - __VITE_ADDITIONAL_SERVER_ALLOWED_HOSTS
    env_file:
      - /run/base44/app.env
